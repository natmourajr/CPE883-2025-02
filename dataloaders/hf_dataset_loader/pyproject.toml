[project]
name = "hf_dataset_loader"
version = "0.1.0"
description = "Data Loader for Huggingface Hub datasets"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=2.0.0",
    "transformers>=4.44.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.4.1",
]
